name: JavaScript Code Linter for ProgramCodeModified.js

on:
  push:
    branches:
      - main

jobs:
  LintProgramCode:
    runs-on: ubuntu-latest
    steps:
      # Клонування репозиторію
      - name: Checkout repository
        uses: actions/checkout@v3

      # Встановлення Biome
      - name: Install Biome
        run: npm install --save-dev --save-exact @biomejs/biome

      # Перевірка тільки ProgramCodeModified.js
      - name: Lint ProgramCodeModified.js
        run: npx @biomejs/biome check 2.6-GitHubActions/ProgramCodeModified.js
